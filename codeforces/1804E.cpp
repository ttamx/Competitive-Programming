#include<bits/stdc++.h>

using namespace std;

const int N=25;

int n,m;
vector<int> adj[N];
vector<pair<int,int>> e;
int deg[N];
bool del[N];

int main(){
    cin.tie(nullptr)->sync_with_stdio(false);
    cin >> n >> m;
    e.resize(m);
    for(auto &[u,v]:e)
    for(int i=0;i<(1<<n);i++){
        for()
    }
}