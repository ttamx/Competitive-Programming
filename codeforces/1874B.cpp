#include<bits/stdc++.h>

using namespace std;

void runcase(){
    int a,b,c,d,m;
    cin >> a >> b >> c >> d >> m;
    map<pair<int,int>,bool> vis;
    queue<pair<int,int>>
}

int main(){
    cin.tie(nullptr)->sync_with_stdio(false);
    int t(1);
    cin >> t;
    while(t--)runcase();
}