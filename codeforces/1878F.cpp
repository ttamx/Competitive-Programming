#include<bits/stdc++.h>

using namespace std;

typedef long long ll;

const ll lim=2e6+1;

void runcase(){
    ll n;
    int q;
    cin >> n >> q;

}

int main(){
    cin.tie(nullptr)->sync_with_stdio(false);
    int t(1);
    cin >> t;
    while(t--)runcase();
}