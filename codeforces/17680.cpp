#include<bits/stdc++.h>

using namespace std;

void runcase(){
    int k;
    cin >> k;
    cout << k-1 << '\n';
}

int main(){
    cin.tie(nullptr)->sync_with_stdio(false);
    int t(1);
    cin >> t;
    while(t--)runcase();
}