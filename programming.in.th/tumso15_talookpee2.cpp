#include<bits/stdc++.h>

using namespace std;

typedef long long ll;

void runcase(){
    ll n,p;
    cin >> n >> p;
    cout << 0 << '\n';
}

int main(){
    cin.tie(nullptr)->sync_with_stdio(false);
    int t;
    cin >> t;
    while(t--)runcase();   
}