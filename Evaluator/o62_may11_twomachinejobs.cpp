#include<bits/stdc++.h>

using namespace std;

typedef long long ll;

int n,t,m1,m2;

struct treap{
    struct node{
        ll ans,mx,x,y;
        node *l,*r;
        node(ll x,ll y):
    };
}tr;

int main(){
    cin.tie(nullptr)->sync_with_stdio(false);
    
}