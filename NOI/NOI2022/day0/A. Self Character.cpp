#include<bits/stdc++.h>

using namespace std;

int n;
int freq[30];
string s;

int main(){
	cin.tie(nullptr)->sync_with_stdio(false);
	cin >> n;
	cin >> s;
	string ans=s;
	for(auto c:s)++freq[c-'A'];
	ans="";
	for(int i=0;i<n;++i){
        if()
	}
    cout << ans;
	return 0;
}

